# cloudfusion
CloudFusion Operator is a Kubernetes-native multi-cloud VM provisioning operator written in Go that dynamically creates and manages virtual machines across AWS and Microsoft Azure based on user-defined CR. It enables declarative infrastructure provisioning through Kubernetes, bringing multi-cloud VM lifecycle management into a single control plane.
